name: {{.ProjectName}}
description: "A Flutter Gopher-generated plugin for bridging Flutter, Golang, and Platform code."
version: 0.0.1
homepage:

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  protobuf: ^3.1.0

flutter:
  plugin:
    platforms:
      android:
        package: {{.PackageName}}
        pluginClass: {{.PluginClassName}}
        dartPluginClass: {{.LibClassName}}
      ios:
        pluginClass: {{.PluginClassName}}
        dartPluginClass: {{.LibClassName}}
      linux:
        ffiPlugin: true
        dartPluginClass: {{.LibClassName}}
      macos:
        pluginClass: {{.PluginClassName}}
        dartPluginClass: {{.LibClassName}}
      windows:
        ffiPlugin: true
        dartPluginClass: {{.LibClassName}}