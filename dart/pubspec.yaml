name: flutter_gopher
description: "A core component in Flutter plugins generated by the Flutter Gopher tool, providing FFI binding support between Flutter applications and Go backends."
version: 0.0.1
repository: https://github.com/czg99/flutter_gopher

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0